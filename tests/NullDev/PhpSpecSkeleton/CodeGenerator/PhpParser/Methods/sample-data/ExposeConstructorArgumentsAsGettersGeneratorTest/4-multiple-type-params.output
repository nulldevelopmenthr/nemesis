<?php

public function it_should_expose_constructor_arguments_via_getters(FirstName $first, SecondName $second, LastName $last)
{
    $this->getFirst()->shouldReturn($first);
    $this->getSecond()->shouldReturn($second);
    $this->getLast()->shouldReturn($last);
}