<?php

namespace spec\Something;

use PhpSpec\ObjectBehavior;
use Prophecy\Argument;
use Something\UserCommandHandler;
use Something\UserId;
use Something\UserModel;
use Something\UserRepository;
class UserCommandHandlerSpec extends ObjectBehavior
{
    public function let(UserRepository $repository)
    {
        $this->beConstructedWith($repository);
    }
    public function it_is_initializable()
    {
        $this->shouldHaveType(UserCommandHandler::class);
    }
    public function it_should_expose_constructor_arguments_via_getters()
    {
    }
}