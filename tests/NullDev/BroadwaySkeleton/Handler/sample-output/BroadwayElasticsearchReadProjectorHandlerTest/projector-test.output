<?php

declare(strict_types=1);

namespace Tests\Something;

use Mockery;
use Mockery\Adapter\Phpunit\MockeryPHPUnitIntegration;
use Mockery\MockInterface;
use PHPUnit\Framework\TestCase;
use Something\UserFactory;
use Something\UserProjector;
use Something\UserRepository;

/**
 * @covers \Something\UserProjector
 * @group todo
 */
class UserProjectorTest extends TestCase
{
    use MockeryPHPUnitIntegration;
    /** @var MockInterface|UserRepository */
    private $repository;
    /** @var MockInterface|UserFactory */
    private $factory;
    /** @var UserProjector */
    private $sut;

    public function setUp()
    {
        $this->repository = Mockery::mock(UserRepository::class);
        $this->factory    = Mockery::mock(UserFactory::class);
        $this->sut        = new UserProjector($this->repository, $this->factory);
    }

    public function testNothing()
    {
        $this->markTestIncomplete('Auto generated using nemesis');
    }
}
